<Window x:Class="Autocorrect.App.SuggestionPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="280"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False"
        Focusable="False"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="#111827"
            BorderBrush="#334155"
            BorderThickness="1"
            CornerRadius="8"
            Padding="7"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18"
                              ShadowDepth="5"
                              Color="#000000"
                              Opacity="0.42" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="Suggestions"
                       Foreground="#94A3B8"
                       FontSize="11"
                       Cursor="SizeAll"
                       MouseLeftButtonDown="SuggestionHeader_OnMouseLeftButtonDown"
                       Margin="6,2,6,4" />
            <ItemsControl x:Name="SuggestionItems">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Tag="{Binding Text}"
                                Loaded="SuggestionButton_OnLoaded"
                                Click="Suggestion_OnClick"
                                HorizontalContentAlignment="Stretch"
                                Background="#1F2937"
                                BorderBrush="#334155"
                                Padding="8,6"
                                Margin="0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Text}"
                                           FontSize="15"
                                           FontWeight="SemiBold"
                                           Foreground="#F8FAFC" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Reason}"
                                           Foreground="#94A3B8"
                                           FontSize="11"
                                           Margin="10,2,0,0" />
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
